/**
 * sessionStorage key the Group Comparison modal uses to hand the entered list
 * URLs to the dedicated /group/results page (issue #6). Kept here so the writer
 * (GroupModal) and the reader (results page) can't drift apart.
 */
export const GROUP_URLS_KEY = "group-compare-urls";
